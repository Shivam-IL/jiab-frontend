// Notification transformers
// Add any data transformation functions here if needed

export const transformNotificationData = (rawData: unknown) => {
  // Transform raw notification data if needed
  return rawData;
};
